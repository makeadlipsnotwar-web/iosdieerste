name: flash
description: Eine Flutter-App, die nur main.dart enthält.
publish_to: "none" # Keine Veröffentlichung auf pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Hier kannst du später weitere Pakete eintragen, z.B.:
  # cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
